# CS2 Dataset Collection Requirements

# Core dependencies
demoparser2>=0.28.0
pandas>=2.0.0
tqdm>=4.65.0

# Recording dependencies
obsws-python>=1.5.0
pyautogui>=0.9.54
keyboard>=0.13.5

# Audio recording (WASAPI)
# Note: audio_capture.py should already be in this folder

# Optional: for video frame extraction
opencv-python>=4.8.0

# For JSON parsing
# (built-in: json, os, pathlib, typing)
